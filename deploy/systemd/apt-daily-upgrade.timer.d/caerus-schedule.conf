[Timer]
# Run unattended upgrades after package metadata refresh and before the
# production runway. Automatic reboot remains disabled by Ubuntu policy.
OnCalendar=
OnCalendar=*-*-* 02:15:00 America/New_York
RandomizedDelaySec=15m
Persistent=true
