[Timer]
# Keep package metadata work outside the 06:45-10:00 trading runway and the
# 18:30-21:00 post-close chain. Automatic package maintenance remains enabled.
OnCalendar=
OnCalendar=*-*-* 01:15:00 America/New_York
RandomizedDelaySec=15m
Persistent=true
