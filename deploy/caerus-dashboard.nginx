server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /var/www/caerus-dashboard;
    index index.html quant_daily_executive.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
