name: Daily Alpaca Paper Run (Deprecated Wrapper)

on:
  workflow_dispatch:
    inputs:
      report_date:
        description: "Override REPORT_DATE (YYYY-MM-DD)."
        required: false
        type: string

jobs:
  deprecated_wrapper:
    runs-on: ubuntu-latest
    steps:
      - name: Stop deprecated wrapper usage
        shell: bash
        run: |
          echo "::error::daily-alpaca-paper.yml is deprecated."
          echo "::error::Use VM cron in scripts/crontab.txt for precompute, execution, and confirmation."
          exit 1
