# September 14 precompute recovery

Brett authorized one premarket recovery precompute and testing the remaining scheduled PAPER plumbing on September 14. The 05:00 scheduled failure remains a failed day for the five-session clean streak. An in-process, date-bounded admission exception preserves actual timestamps and changes neither installed cron admission nor execution authority.

The recovery passed Orion dependency verification, then failed before publishing the bundle contract because Aquila's monthly-state reader did not consume the existing governed two-parent recovery chain. Friday's P0 and P1 failed receipts remain failed historical attempts; the successful P2 exact plan embeds their closed ownership and economic reconciliation chain. The reader now verifies that existing chain and its immutable source/WAL evidence before treating only those parents as resolved by the later successful successor. It does not relabel failed attempts as successes or accept unrelated or later failures.

The September 14 Orion compatibility bridge binds every direct predecessor edge from the exact newly reviewed runtime to the unchanged Friday producer. The whole runtime content hash and immutable Friday marker, source, hydration, date and lineage checks still apply.

Completion may resume from the exact saved three-member partial bundle under the canonical execution manifest. Preserve the original producer failure and planning inputs, create the missing source/evaluation/contract, then use normal sealing, bundle validation and no-submit readiness certification. A valid recovered bundle may be consumed by the already-authorized 09:35 PAPER job with fresh execution checks. This recovery grants no Live activation, added capital, promotion or relaxation of safety gates.

Evidence and exact stage authority are recorded in `atlas/outputs/maintenance/2026-09-14/one_off_precompute/` and the existing central execution manifest in the parent workspace. A successful recovery is distinct from a clean scheduled session.
