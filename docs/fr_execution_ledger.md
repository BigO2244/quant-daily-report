# Caerus FR Execution Ledger

## Purpose

This ledger preserves immutable operational memory for Friday Refactor (FR)
execution. It records what was completed, how it was validated, whether rollback
was considered, and whether documentation was updated.

Do not rewrite history to make prior work look cleaner. Add corrective notes in
new rows when later audits refine the state.

| Date | FR | Status | Commit | Validation | Rollback Verified | Docs Updated | Notes |
|---|---|---|---|---|---|---|---|
| 2026-05-04 | FR-003 managed ticker exceptions | DONE_LOCAL_WIP | pending local commit | Planned: `pytest Tests/test_price_cache_only_hydrator.py Tests/test_ticker_exceptions.py -q`; dry-run hydration. | Config rollback is to empty or remove ticker exceptions. | `docs/price_hydration.md` present in local WIP. | Implemented in local WIP, not yet deployed from canonical git at time of ledger creation. Do not mark deployed until committed, pushed, pulled on VM, and validated. |
| 2026-05-08 | FR-008 deployment reconciliation/governance recovery | DEPLOYED | `c8c0e10` plus ledger/status follow-up | VM backup, stash, fetch, `git merge --ff-only origin/main`, governance commit push, VM `git pull --ff-only`, `git status`, `git log -1 --oneline`, `bash -n` on changed shell scripts. | Yes: timestamped VM backup patches and retained stashes preserved; no stash entries dropped. | `AGENTS.md`, `docs/deployment_workflow.md`, `docs/documentation_governance.md`, `docs/fr_execution_ledger.md`, `docs/friday_refactor_backlog.md`, `docs/OPERATIONS.md`, `docs/runbook.md`, `docs/CODEX_AUDIT_TARGETS.md`. | Deterministic VM git deployment restored and validated end-to-end without SCP. Local non-governance WIP remains intentionally deferred outside FR-008. |
| 2026-05-15 | FR-004 feedback-loop rolling index | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_feedback_loop_artifacts.py -q`; combined FR slice. | Additive artifact rollback is to stop calling `_write_rolling_index` and leave dated JSON artifacts canonical. | `docs/feedback_loop_artifacts.md`. | Writes compact CSV/JSON rows under `outputs/shadow_candidates/performance/`; no execution, broker, reconciliation, or scheduler behavior changed. |
| 2026-05-15 | FR-006 required vs optional artifact health | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_portfolio_learning_report.py -q`; combined FR slice. | Restore single artifact-health classification in `core/portfolio_learning_report.py`. | `docs/feedback_loop_artifacts.md`. | Missing optional learning artifacts remain visible but no longer make the core scoreboard unavailable by themselves. |
| 2026-05-15 | FR-009 GitHub Actions SHA pinning | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_operational_validation.py -q`; `.venv/bin/python3 scripts/operational_validation.py`; workflow YAML parse. | Restore prior tag-based action refs only if a pinned SHA proves invalid. | `docs/operational_validation.md`. | All workflow `uses:` refs validate as pinned 40-character SHAs. |
| 2026-05-15 | FR-011 workflow permission minimization | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_operational_validation.py -q`; `.venv/bin/python3 scripts/operational_validation.py`. | Restore prior workflow permission blocks if workflow writes fail. | `docs/operational_validation.md`. | Workflow-scope `contents: write` removed; commit-capable workflows retain job-level elevation only. |
| 2026-05-15 | FR-013 dependency monitoring governance | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_operational_validation.py -q`; `.venv/bin/python3 scripts/operational_validation.py`. | Remove or disable `.github/dependabot.yml` if advisory PR noise is unacceptable. | `docs/dependency_governance.md`, `docs/operational_validation.md`. | Adds advisory-first Dependabot monitoring for pip and GitHub Actions without auto-merge. |
| 2026-05-15 | FR-001 shadow wrapper decomposition | DONE_LOCAL_WIP | pending local commit | `.venv/bin/python3 -m pytest Tests/test_shadow_daily_wrapper.py -q`; `bash -n scripts/run_shadow_candidates_daily.sh`. | Restore prior inline wrapper body and remove per-step status artifacts. | Ledger entry only. | Top-level wrapper remains non-blocking and now writes per-step generate/latest/reconciliation status artifacts. |
