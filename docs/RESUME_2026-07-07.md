# Resume State - 2026-07-07

## Execution-Core Chain

Branch chain:

1. `codex/execution-core-1b` at `1d6a5490ed157ec426d7c6586f1cc691e2d07b78`
   - Unified execution core extracted behind the parity harness.
   - Paper lifecycle implemented as canonical behavior.
   - Parity goldens passed, including `2026_07_07_real`.
2. `codex/paper-on-core-1c` at `f3716a971e466e6e01e716f91d6a462d5e484d02`
   - Paper production path routed through `execution/core.py`.
   - Real `2026_07_07` golden reproduced through the production path.
   - Native paper fallback remains present and must not be deleted yet.
3. `codex/live-on-core-1d` at settlement fix commit `a1937bf4daf76f09a73b64e0159cb21a76f99b71`
   - Live path routed through `execution/core.py`.
   - Mock rotation path proves sell -> settlement refresh -> rebudget -> buy.
   - Settlement fail-closed behavior added for unsettled sells.
   - Settlement-cash-reflection fix requires refreshed cash and buying_power to reflect at least 95% of expected sell proceeds before live buys are allowed.
   - Fresh adversarial re-review after the fix found no confirmed/high live-path findings.

## Not Done

- The execution-core chain has not been merged to `main`.
- The native paper fallback is still present; do not delete it yet.
- Live is not armed.
- The VM is still expected to be on `31829cf5bd5a0732123d85b99987f193453d2f4c`, which has the ledger and Workstream C but not the rotation-capable live-on-core chain.
- No VM verification or VM write was performed during this freeze.
- The kill switch remains engaged and must not be changed without explicit by-hand approval.

## Tomorrow's Decisions

1. Merge the execution-core chain to `main` behind the parity gate.
2. VM fetch with kill-switch-in-situ checks, using extra care because the fetched code is rotation-capable.
3. Layer 2: canonical P/L plus Alpaca reconciliation.
4. Unified paper plus live email.
5. Real-broker settlement validation before arming.
6. Arm live only by an explicit by-hand kill-switch flip.

## Parity Anchor

Any future change to `execution/core.py`, the paper adapter, the live adapter, or the parity harness must still reproduce `2026_07_07_real` exactly:

- `ending_cash = 939.73`
- `target_cash_weight = 0.0875652909`
- buys: `PNC`, `GE`
- skipped: `VRTX`, `WELL`, `ALL`, `ABBV`, `AVGO`, `ELV`, `NXPI`, `F`, `PANW`, `GEV`
