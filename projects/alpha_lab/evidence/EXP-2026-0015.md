# EXP-2026-0015 Evidence Gap Packet

Date: 2026-08-30
Hypothesis: `HYP-2026-015`
Classification: `UNPROVEN`
Run state: `REVIEW`
Gate outcome: `BLOCKED_DATA`
Owner lifecycle decision: `NOT_REQUESTED`

## What was run

Brett authorized the frozen experiment with `RUN EXPERIMENT`. The first
prespecified step was executed as a no-return source gate against the exact GCP
inputs bound in the frozen hypothesis. The gate stopped at the first failed
control, before reporter reaction, peer eligibility, market returns, validation
outcomes, statistical inference, or the challenge period could be read.

- Frozen spec SHA-256:
  `3ca51f2f477c548d0b9ad266f004b4f61ba532f1d23961847c05db1e5fd033d6`.
- Gate runner commit: `3d4145fbad13d351bc61ebb1ab4efbac394ecd29`.
- Gate runner SHA-256:
  `5b09b761116661d9d59fbe0bd577e5e5ce7b439033a555d73a127317de9fec46`.
- GCP run ID: `20260830T145245Z-hyp-2026-015-data-gate-v1`.
- GCP evidence root:
  `outputs/research/alpha_lab/HYP-2026-015/20260830T145245Z-hyp-2026-015-data-gate-v1/`.

## Failed control

The frozen original-filings bundle is internally consistent with its manifest,
but it is not complete:

| Check | Required | Observed | Result |
|---|---:|---:|---|
| Frozen source-manifest hash | exact | exact | `PASS` |
| Frozen bundle hash | exact | exact | `PASS` |
| Candidate original filings | 313,450 | 313,450 | `PASS` |
| Hydrated original filings | 313,450 | 313,449 | `FAIL` |
| Acceptance-time pass count | 313,450 | 313,449 | `FAIL` |
| Source errors | 0 | 1 | `FAIL` |
| Missing original in Item 2.02 tape | 0 | 1 | `FAIL` |

The missing filing is accession `0001549848-14-000068`, issuer CIK
`0001549848`, accepted `2014-08-11T16:14:19Z`. Its expected SEC source path is
`edgar/data/1549848/000154984814000068/0001549848-14-000068.txt`. The discovery
tape contains that Item 2.02 event but has no source hash because the original
bytes are absent.

This is a terminal failure for the current gate because the frozen contract
requires 100% original-source and exact acceptance-time coverage. Dropping the
row, assuming its contents, or continuing to returns would violate the
preregistered missing-data rule.

## Controls deliberately not inspected

The following controls remain unproven, not failed:

- exact acceptance-time retention for every retained Item 2.02 original;
- unique reporter and eligible-peer identity mapping;
- validation cluster, unique-peer, and four-digit-SIC floors;
- deterministic overlap handling;
- reaction and five-session holding-path completeness; and
- terminal-event disposition.

The source gate short-circuited before those stages. No market return, reporter
reaction, validation outcome, or challenge input was accessed. No statistical
trial was opened.

## Separate outcome-stage blockers

Even after repairing the source bundle, outcome access remains blocked by
governance and two frozen-contract ambiguities:

1. The authenticated canonical global research ledger is absent from canonical
   local and GCP Alpha Lab. Existing unmerged ledger code also rejects the
   frozen family, wave, and challenge identifier formats used by HYP-2026-015.
2. The frozen spec does not define how multiple same-SIC reporters on the same
   reaction session aggregate reporter shock/volume, construct the reporter
   baseline, or identify the reporter component of the inference cluster.
3. The frozen spec applies symmetric costs to candidate and industry baseline
   portfolios but does not freeze event capital and cash allocation, leaving
   the intended incremental cost calculation underdetermined.

None of these terms was silently chosen during this run.

## Exact evidence

| Artifact | SHA-256 |
|---|---|
| `result.json` | `451c719893b868856c73d4ee75110707dd971a9b11a35f0e29eabfb851a73178` |
| `receipt.json` | `bb46bf5c4b1e794de30a7b0f9000b2a2443af8d934bff8e32fa9a5595616f734` |
| `events.jsonl` | `68da567fb1c73ae76f6ab9bb2efc9bd224012635f8ec8a5bc0b64de77246be1b` |

The hash-chained run event store contains exactly two events. Three targeted
tests passed on GCP, covering fail-closed Item 2.02 source handling, create-only
run directories, and rejection of frozen-manifest drift.

## Data recovery routes and acceptance tests

The cheapest owned/free route is to retry the exact accession from SEC Archives
with the governed SEC user agent, preserve the original bytes, parse the SEC
header acceptance time, and publish a new immutable source bundle. SEC index or
bulk metadata may corroborate identity and timing but cannot substitute for the
missing original bytes. A non-SEC copy is honest only as an explicitly labeled
exploratory proxy unless byte identity and provenance can be proven.

Recovery acceptance requires:

1. the exact accession resolves to immutable original bytes with a non-null
   SHA-256 and preserved SEC header;
2. the new bundle reports candidate, hydrated, and acceptance-time counts all
   equal, with zero source errors;
3. the Item 2.02 tape maps the event to that exact original and hash;
4. an owner-approved frozen addendum or new generation binds the replacement
   source snapshot and resolves the two evaluator ambiguities; and
5. the authenticated ledger is reconciled and wave/family/experiment records
   are appended before any outcome-bearing input is read.

Only then should the no-return gate be rerun. The 2025-01-01 through 2026-06-30
challenge epoch remains sealed and requires separate later authorization.

## Boundary attestation

This was research-only work. It did not change Alpha Sunday automation,
production source, cron, broker state, capital authority, strategy lifecycle,
Lyra Live, Orion Paper, or any order path.
